<UserControl x:Class="MilitaryFaculty.Presentation.Views.ImagedCommandView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300"
             Background="White">
    <Grid>
        <Button Style="{StaticResource ImagedButtonStyle}"
                Command="{Binding Path=Model}"
                CommandParameter="{Binding Path=Parameter}">
            <Button.ToolTip>
                <TextBlock Text="{Binding Path=Tooltip}"/>
            </Button.ToolTip>
            <Image Source="{Binding Path=ImageSource}" 
                   Style="{StaticResource CommandImageStyle}"/>
        </Button>
    </Grid>
</UserControl>
